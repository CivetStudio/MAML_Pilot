<Template>

	<Props>
		<item name="condition" type="string" description="条件判断" default="$#tc$" />
	</Props>

    <!-- 西瓜视频 -->
    <IntentCommand condition="($condition)" action="android.intent.action.MAIN" package="com.ss.android.article.video" class="com.ss.android.article.video.activity.SplashActivity" />

</Template>