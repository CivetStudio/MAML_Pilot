<Template>

	<Props>
		<item name="condition" type="string" description="条件判断" default="$#tc$" />
	</Props>

	<!-- Keep -->
	<IntentCommand condition="($condition)" action="android.intent.action.MAIN" package="com.gotokeep.keep" class="com.gotokeep.keep.splash.SplashActivity" />

</Template>